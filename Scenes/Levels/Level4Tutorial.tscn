[gd_scene load_steps=2 format=2]

[ext_resource path="res://Scenes/TutorialScreen/TutorialScreen.tscn" type="PackedScene" id=1]

[node name="TutorialScreen" instance=ExtResource( 1 )]

[node name="TutorialText" parent="Control/NinePatchRect/MarginContainer/PanelContainer" index="0"]
text = "This flower has grown itself into a bit of a corner. Help it find the nutrients by pruning the vines that only crowd the area.

New growth prefers spots with no neighboring vines. Only when a flower has run out of all other options will it fill in tight spaces."
